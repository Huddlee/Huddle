services:
  app:
    build: .
    container_name: huddle-backend
    ports:
      - "${SERVER_PORT:-8080}:${SERVER_PORT:-8080}"
    env_file:
      - .env
    # This guarantees the container can ping your host machine's localhost
    extra_hosts:
      - "host.docker.internal:host-gateway"